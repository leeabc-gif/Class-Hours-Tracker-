# v1.1.4 线上升级操作指引（走 CNB 通道）

> 发布日期：2026-09-11　平台：**CNB（已发布并线上校验通过）**
> 关键背景：你的站点默认更新源是 GitHub，但本版按你的选择**只发到了 CNB**。
> 所以升级前需要在后台把「在线更新源」从 GitHub 切到 CNB（一次性操作）。

---

## 一、先确认你站点当前版本

后台首页 / 系统更新页能看到当前版本号。对照下面两条路径。

## 二、把更新源切到 CNB（一次性，所有版本通用）

1. 进入后台 **系统设置**（基础配置页）。
2. 找到 **「在线更新源」** 下拉框。
3. 从 `GitHub Releases（默认推荐）` 改为 **`CNB 官方 Release（v1.0.1 兼容通道）`**。
4. **「更新清单地址」留空**（留空即用 CNB 默认 latest 入口，系统会自动解析出最新版）。
5. 保存设置。

> 保存后，「系统更新」页顶部会显示：更新源 `CNB 官方 Release（默认）`。

## 三、按当前版本选择升级方式（重要，别跳级）

本版 `upgrade.sql` 含建表 DDL。**执行升级的是站点上现有的更新器**，旧更新器遇到 DDL 会死锁
（报 `There is no active transaction`）。因此必须保证先过 v1.1.3 垫脚石。

### 路径 A：当前是 **1.1.3 或更高**
- 直接进「系统更新」→ 检查更新 → 看到 **v1.1.4** → 点「立即更新」即可。
- 升级会自动建好 7 张表、提升额度精度，无需手工导 SQL。

### 路径 B：当前是 **1.0.x / 1.1.0 / 1.1.1 / 1.1.2**
必须**分两步**，不能跳：
1. 切到 CNB 源后点检查更新。系统会提示
   「该更新包要求从 v1.1.3 起升，当前 vX.X.X 跨度过大，请先升到中间版本。」——这是**正常保护**。
2. 先把目标指向 **v1.1.3** 垫脚石（它 `from_version=1.0.0`，任何老站都能升）：
   - 在「更新清单地址」临时填 v1.1.3 的固定清单：
     `https://cnb.cool/bmayan/class-hours-tracker/-/releases/download/v1.1.3/manifest.json`
   - 检查更新 → 升到 **v1.1.3**（纯 DML，旧更新器也能安全跑完）。
3. 升完 v1.1.3 后，**把「更新清单地址」清空**（恢复用 CNB 默认 latest），保存。
4. 再次检查更新 → 这次得到 **v1.1.4** → 点「立即更新」。

> 为什么这样设计：v1.1.3 装完后，站点上就是**修复版更新器**，它能正确处理 v1.1.4 的建表 DDL。

## 四、升级后验证

- 「系统更新」页当前版本显示 **1.1.4**，提示已是最新。
- AI 中转 / 通知公告相关菜单正常打开（依赖新表）。
- 如想看表是否建好：数据库应从原来的 12 张增加到 **19 张**
  （新增 `ks_ai_channel / ks_ai_model / ks_ai_token / ks_ai_quota / ks_ai_usage_log / ks_notice / ks_notice_read`）。

## 五、出问题怎么办

- 在线更新在应用前会自动做**文件备份 + 整库备份**，失败会自动回滚文件。
- 若提示需要数据库备份恢复，用升级前自动生成的整库备份即可。
- 本次 DDL 为 `CREATE TABLE IF NOT EXISTS` + 幂等 `ALTER`，重复执行安全。

---

### 附：本版发布校验结果（已在 CNB 线上实测）
- CNB Release id `2098289766013145088`，tag `v1.1.4` 指向提交 `c4a779a`。
- 线上 latest 解析 → `v1.1.4`；manifest 合法、URL 指向 CNB。
- 线上 zip（793575 字节）sha256 与清单一致：`353115463004c4ff1bf2ea976b84d43bc8af705ea27b0e4e1afcb18da77650de`。
- RSA-SHA256 用出厂公钥验签通过。
- v1.1.3 垫脚石资产同样完好（`from_version=1.0.0`）。
- 隔离库真实升级测试：12 → 19 表、`app_version=1.1.4`、幂等重跑通过。
